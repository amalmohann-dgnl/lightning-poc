import { PageTransition } from "../models";

const PAGETRANSITION = {
    LEFT: 'left' as PageTransition,
    RIGHT: 'right' as PageTransition,
    UP: 'up' as PageTransition,
    DOWN: 'down' as PageTransition,
    FADE: 'fade' as PageTransition,
    CROSSFADE: 'crossFade' as PageTransition,
}

export default PAGETRANSITION;